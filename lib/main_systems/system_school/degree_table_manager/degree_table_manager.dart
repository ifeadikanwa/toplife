class DegreeTableManager{
  static void createDegreeTable(){
    //if there is content in the degree table, check if every degree discipline and branch matches a record in the degree table.
    //if a match isn't found for any degree discipline branch then:
    //clear all records from the degree table if it already exists
    //go through every degree discipline and insert it in the degree table
    //if there is no content in the degree table go straight to loading content into it
  }

  static void loadDegreesIntoDegreeTable() {
    //method used in create table whose job is to clear the degree table and load every degree branch into the degree table
  }
}