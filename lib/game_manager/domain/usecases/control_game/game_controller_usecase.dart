import 'package:toplife/core/data_source/drift_database/database_provider.dart';

class GameControllerUsecase {
  Future<void> execute({
    required Game oldGame,
    required Game newGame,
  }) async {
    // print("OLD GAME: $oldGame");
    // print(("NEW GAME: $newGame"));
  }
}
